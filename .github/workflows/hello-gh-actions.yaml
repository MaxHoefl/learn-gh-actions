name: Hello Github Actions
run-name: ${{ github.actor }} is saying hello 👋
on:
  push:
    branches:
      - main

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "This is my first GH Actions workflow 🔥"
        
